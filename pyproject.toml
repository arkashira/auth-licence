[project]
name = "auth-licence"
version = "0.1.0"
description = "Simple anti‑piracy incident logger with SQLite persistence."
requires-python = ">=3.9"
authors = [{name = "Axentx AI"}]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
